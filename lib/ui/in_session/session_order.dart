import 'package:flutter/material.dart';

class SessionOrder extends StatelessWidget {
  const SessionOrder({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("Session Order");
  }
}
